server:
  context-path: /micro
  port: 8080
  
spring:
  cloudant:
    username: ${vcap.services.lightblue-cloudant-timo.credentials.username}
    password: ${vcap.services.lightblue-cloudant-timo.credentials.password}
    host: ${vcap.services.lightblue-cloudant-timo.credentials.host}
    protocol: https
    port: 443
    database: customers